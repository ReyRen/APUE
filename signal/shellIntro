1.shell其实就是个命令解析器，在/bin下面有，和bin下的其他文件一样，shell其实也就是个应用程序，只不过是用于解析命令的应用程序
2./bin目录（binary）是二进制执行文件目录，主要用于具体应用
 /sbin 目录（system binary）是系统管理员专用的二进制代码存放目录，主要用于系统管理
3. ls执行过程：
	ls命令先传给shell，然后shell传给相对应的内核函数，然后内核函数调用相应的.c然后打印出来
4.bash的特点：速度快，上下键，tab补齐

